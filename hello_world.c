#include <stdio.h>  //including the standard input output to print the message in the terminal

int main() 
{
    printf("Hello, world!\n"); //prints Hello, world! in the terminal then moves to a new line to make it look cleaner
    return 0; 
}